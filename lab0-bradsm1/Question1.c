#include <stdio.h>
#include <stdlib.h>

#include "Questions.h"



int addFunction(int input1, int input2){
    int sum; //Creates the variable that holds the sum.
    sum = input1 + input2; // adds the two input variables.
    return sum; //returns the sum variable containing the sum of inputs.
}
